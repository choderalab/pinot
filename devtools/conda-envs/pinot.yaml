name: test
channels:
    - conda-forge
dependencies:
    # Base dependencies
  - python
  - pip

    # Testing
  - pytest
  - pytest-cov
  - codecov
  
  - rdkit

    # Pip-only installs
  - pip:
    - pytorch
    - dgl
    - matplotlib
    

